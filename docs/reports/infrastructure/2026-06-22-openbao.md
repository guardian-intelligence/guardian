# OpenBao Operational Report

## Scope

- Component: OpenBao.
- Desired state source: `src/infrastructure/base/openbao/`,
  `src/infrastructure/evidence/openbao-load.yaml`.
- Cluster: `guardian-mgmt`.
- Environment or tenant: `tenant-root`.
- Report date: 2026-06-22.
- Status: pending live execution.

## Preflight

- Render/build validation: `aspect infra preflight`.
- Reconciled Kubernetes resources: `OpenBAO/tenant-root/guardian`,
  `CiliumNetworkPolicy/tenant-root/allow-openbao-to-apiserver`,
  `Secret/tenant-root/guardian-openbao-evidence-token`,
  `Job/tenant-root/evidence-openbao-load`.
- Healthy baseline command: `aspect infra live-snapshot --kubeconfig "${KUBECONFIG}"`.
- Result: pending.

## Load Test

- Command:
  `aspect infra seed-openbao-evidence-token --kubeconfig "${KUBECONFIG}"`,
  `aspect infra evidence-apply --kubeconfig "${KUBECONFIG}"`,
  `aspect infra evidence-wait --kubeconfig "${KUBECONFIG}" --timeout 30m`,
  `aspect infra evidence-logs --kubeconfig "${KUBECONFIG}"`, and
  `aspect infra live-snapshot --kubeconfig "${KUBECONFIG}"`.
- Target: `http://guardian.tenant-root.svc:8200`.
- Inputs: 25 KV v2 writes and reads under
  `kv/guardian/evidence/openbao`.
- Pass criteria: evidence log reports `openbao-load total=25 failures=0`, no
  pod restart, all replicas stay Ready, and raft quorum is intact.
- Result: pending.

## Disaster Recovery Drill

- Failure injected: delete one OpenBao Pod and then power off one node.
- Restore source: raft replica set and `replicated-retain` PVCs.
- Pass criteria: raft quorum survives, the replacement Pod rejoins, and a
  post-drill read returns the pre-drill value.
- Result: pending.

## Single-Node Outage Exercise

- Procedure: run the OpenBao read/write check before, during, and after a
  single-node outage.
- Expected behavior: OpenBao remains available with quorum; total cluster loss
  recovery is explicitly not required for this phase.
- Result: pending.

## Residual Risk

- OpenBao secret projection remains a known platform gap; current delivery is
  secret-zero plus Kubernetes Secrets.
