/bazel-*
secret.env
/src/aisucks/aisucks

# Archived pre-Cozystack tree. Keep source, not local dependency/build output.
/src-old/**/node_modules/
/src-old/**/dist/
/src-old/**/.vite/
/src-old/**/.turbo/
/src-old/**/.next/
/src-old/**/.output/

# OpenTofu/Terraform local state and working directories. Commit
# .terraform.lock.hcl when a stack is initialized so provider checksums are
# reviewable.
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
