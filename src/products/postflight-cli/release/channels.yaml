# Channel pins for the postflight CLI release train. Kargo promotion PRs
# advance these digests (ghcr.io/guardian-intelligence/postflight-cli); the
# in-cluster release reconciler turns each pin move into a GitHub Release
# reusing the signatures minted at build time. Edits by hand defeat the
# train — promote through Kargo instead.
channels:
  nightly:
    image: ghcr.io/guardian-intelligence/postflight-cli@sha256:69b7beae2cd06a4aaae62d629e15be5d6e43a6d1a701b790e1ca3e80c13d4631
