package(default_visibility = ["//visibility:public"])

genrule(
    name = "kubectl",
    srcs = ["@kubectl_linux_amd64//file"],
    outs = ["kubectl-runner"],
    cmd = "cp $(location @kubectl_linux_amd64//file) $@ && chmod +x $@",
    executable = True,
)
