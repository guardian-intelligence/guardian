package(default_visibility = ["//visibility:private"])

# The version-skew conformance test reads the pin to hold it to the cluster
# kubernetesVersion.
exports_files(
    ["kubectl.MODULE.bazel"],
    visibility = ["//src/infrastructure/tests:__pkg__"],
)
