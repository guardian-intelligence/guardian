# The warm-tier writer: subscribes to the cluster-singleton cockpit hub and
# persists 1 s min/max/avg rollups into this stage's product Postgres, where
# Electric serves them as the homepage widget's history shape. One replica:
# writes are idempotent (ON CONFLICT DO NOTHING), and a rollout's gap of a
# second or two in hour-old history is invisible; a second replica would
# just double every write. Contract: src/services/cockpit/rollup.go.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    reloader.stakater.com/auto: "true"
  name: cockpit-rollup
  namespace: tenant-guardian-prod
  labels:
    app.kubernetes.io/name: cockpit-rollup
    app.kubernetes.io/part-of: guardian
    guardian.dev/product: cockpit
    guardian.dev/stage: prod
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: cockpit-rollup
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cockpit-rollup
        app.kubernetes.io/part-of: guardian
        guardian.dev/product: cockpit
        guardian.dev/stage: prod
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: rollup
          image: ghcr.io/guardian-intelligence/cockpit:edge@sha256:8a40b946573ed4392bde3fb5366382723e0397059a748242a7d87bb6ac8a0484 # {"$imagepolicy": "guardian-imageops:cockpit"}
          args:
            - rollup
            - --hub=cockpit-hub.guardian-cockpit.svc:80
          env:
            - name: PGHOST
              value: postgres-products-rw.tenant-guardian-prod.svc
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: cockpit
            - name: PGUSER
              value: cockpit_writer
            - name: PGSSLMODE
              value: require
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-products-credentials
                  key: cockpit_writer
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 15
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
