filegroup(
    name = "manifests",
    srcs = glob(["*.yaml"]),
    visibility = ["//visibility:public"],
)

# Kept in lockstep with the service's committed sim modules by the diff
# tests in //src/services/mythrad/sim.
exports_files(
    [
        "behavior/client.wasm",
        "behavior/live.wasm",
        "behavior/park.wasm",
    ],
    visibility = ["//src/services/mythrad/sim:__pkg__"],
)
