# Content-addressed dog skins. skins.json picks the default; a skin only
# takes effect once its asset file is present, so snapshots can never
# reference an asset the server cannot serve. (The behavior modules live in
# behavior/ and are assembled by the configMapGenerator in kustomization.yaml
# - wasm bytes can't be inlined as YAML text.)
apiVersion: v1
kind: ConfigMap
metadata:
  name: mythra-assets
  namespace: tenant-guardian-prod
  labels:
    app.kubernetes.io/name: presenced
    app.kubernetes.io/part-of: guardian
    guardian.dev/product: presence
    guardian.dev/stage: prod
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
data:
  skins.json: |
    {"default": "cell"}
