# Keycloak for Guardian customer identity. Hand-rolled Deployment (not the
# official Operator or keycloakx chart — both render StatefulSets, and
# Flagger's targetRef only supports Deployment/DaemonSet). Durable state is
# the stage's CNPG Postgres; multi-pod cache clustering uses the Quarkus
# "kubernetes" stack (DNS_PING against a headless Service, no in-cluster RBAC
# needed, so no ServiceAccount token is mounted).
#
# Flagger interplay (Canary declared in canary.yaml):
# - Flagger owns the apex/primary/canary ClusterIP Services; none is declared
#   here, or Flux and Flagger would fight over the selector.
# - Flagger clones this Deployment to keycloak-primary and suffixes the
#   selector label value, so anything matching pods must match BOTH
#   keycloak and keycloak-primary (network policies, PDB).
# - JGroups discovery must NOT span colors (a mixed-version Infinispan
#   cluster mid-upgrade is exactly what blue/green avoids), so the DNS_PING
#   query is derived from the pod's own app label via fieldRef:
#   keycloak-jgroups for the canary color, keycloak-primary-jgroups for
#   primary — one headless Service per color below.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak
  namespace: tenant-guardian-prod
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: guardian
    app.kubernetes.io/component: iam
    guardian.dev/stage: prod
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: keycloak
  template:
    metadata:
      labels:
        app.kubernetes.io/name: keycloak
        app.kubernetes.io/part-of: guardian
        app.kubernetes.io/component: iam
        guardian.dev/stage: prod
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: keycloak
          image: quay.io/keycloak/keycloak@sha256:3e58dc82a9b72f364f37dac17f99fcaf53d0052a3c1ad7f00b069a1ed43201c0 # 26.6.2
          imagePullPolicy: IfNotPresent
          # start (not start --optimized): the official image augments at
          # startup for KC_DB=postgres; baking an optimized image is a later
          # supply-chain step, not worth a custom build yet.
          args: ["start", "--import-realm"]
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: management
              containerPort: 9000
              protocol: TCP
            - name: jgroups
              containerPort: 7800
              protocol: TCP
          env:
            - name: KC_HTTP_ENABLED
              value: "true"
            # TLS terminates at Cloudflare; ingress-nginx forwards plain HTTP
            # with X-Forwarded-* headers, same as company-site.
            - name: KC_PROXY_HEADERS
              value: xforwarded
            - name: KC_HOSTNAME
              value: https://guardianintelligence.org
            - name: KC_HEALTH_ENABLED
              value: "true"
            - name: KC_METRICS_ENABLED
              value: "true"
            # Sign-in SLO source: per-realm user event counters on the
            # management /metrics endpoint. Events and tags are pinned to
            # keep series cardinality bounded.
            - name: KC_EVENT_METRICS_USER_ENABLED
              value: "true"
            # Failed attempts are counted under the same event value with a
            # non-empty error tag, never as *_error event types.
            - name: KC_EVENT_METRICS_USER_EVENTS
              value: login,logout,code_to_token,client_login
            - name: KC_EVENT_METRICS_USER_TAGS
              value: realm,idp,clientId
            - name: KC_DB
              value: postgres
            - name: KC_DB_URL_HOST
              valueFrom:
                secretKeyRef:
                  name: postgres-keycloak-app
                  key: host
            - name: KC_DB_URL_PORT
              valueFrom:
                secretKeyRef:
                  name: postgres-keycloak-app
                  key: port
            - name: KC_DB_URL_DATABASE
              valueFrom:
                secretKeyRef:
                  name: postgres-keycloak-app
                  key: dbname
            - name: KC_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-keycloak-app
                  key: username
            - name: KC_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-keycloak-app
                  key: password
            - name: KC_CACHE
              value: ispn
            - name: KC_CACHE_STACK
              value: kubernetes
            - name: KC_POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            # The pod's own app label value ("keycloak" or, on the Flagger
            # clone, "keycloak-primary") selects its color's headless
            # Service, keeping each color's Infinispan cluster isolated.
            - name: KC_APP_LABEL
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['app.kubernetes.io/name']
            - name: JAVA_OPTS_APPEND
              value: "-Djgroups.dns.query=$(KC_APP_LABEL)-jgroups.$(KC_POD_NAMESPACE).svc.cozy.local"
            # Used only when startup import creates a fresh Guardian realm.
            # Steady state stores a Vault SPI reference in Keycloak and the
            # reconciler authenticates as the realm-scoped service account.
            - name: REALM_RECONCILER_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-realm-reconciler-client
                  key: password
            - name: TOKEN_GATE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-token-gate
                  key: password
            # File-vault provider: the realm's GitHub IdP clientSecret is a
            # vault expression resolved at USE time from the mounted file
            # below, so the OAuth secret never enters the Keycloak database —
            # and therefore never a database backup. Rotation lands via the
            # kubelet Secret-mount sync, no pod roll needed (env vars would
            # need one).
            - name: KC_VAULT
              value: file
            - name: KC_VAULT_DIR
              value: /opt/keycloak/data/vault
          startupProbe:
            httpGet:
              path: /health/started
              port: management
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health/ready
              port: management
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health/live
              port: management
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
              ephemeral-storage: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
              ephemeral-storage: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: realm-import
              mountPath: /opt/keycloak/data/import
              readOnly: true
            - name: vault
              mountPath: /opt/keycloak/data/vault
              readOnly: true
      volumes:
        - name: realm-import
          configMap:
            name: keycloak-realm-guardian
        # File names follow the file-vault provider's REALM_UNDERSCORE_KEY
        # convention (<realm>_<key>).
        - name: vault
          projected:
            sources:
              - secret:
                  name: keycloak-github-oauth
                  items:
                    - key: GITHUB_CLIENT_SECRET
                      path: guardianintelligence.org_github-client-secret
              - secret:
                  name: postflight-oidc-client
                  items:
                    - key: password
                      path: guardianintelligence.org_postflight-client-secret
              - secret:
                  name: keycloak-realm-reconciler-client
                  items:
                    - key: password
                      path: guardianintelligence.org_realm-reconciler-client-secret
              - secret:
                  name: keycloak-token-gate
                  items:
                    - key: password
                      path: guardianintelligence.org_token-gate-client-secret
---
# Per-color JGroups discovery targets (DNS_PING). publishNotReadyAddresses is
# required: pods must find each other before they report Ready.
apiVersion: v1
kind: Service
metadata:
  name: keycloak-jgroups
  namespace: tenant-guardian-prod
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: guardian
    guardian.dev/stage: prod
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app.kubernetes.io/name: keycloak
  ports:
    - name: jgroups
      port: 7800
      targetPort: jgroups
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: keycloak-primary-jgroups
  namespace: tenant-guardian-prod
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: guardian
    guardian.dev/stage: prod
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app.kubernetes.io/name: keycloak-primary
  ports:
    - name: jgroups
      port: 7800
      targetPort: jgroups
      protocol: TCP
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: keycloak
  namespace: tenant-guardian-prod
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: guardian
    guardian.dev/stage: prod
spec:
  minAvailable: 1
  selector:
    matchExpressions:
      - key: app.kubernetes.io/name
        operator: In
        values:
          - keycloak
          - keycloak-primary
