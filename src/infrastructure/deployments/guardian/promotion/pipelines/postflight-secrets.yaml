# Kargo's git credential for the guardian-postflight project (the shared
# guardian-promotions GitHub App identity). Kargo credentials are
# project-namespaced, so each project materializes its own copy.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: secrets-reader
  namespace: guardian-postflight
  labels:
    app.kubernetes.io/name: kargo
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: guardian-postflight
automountServiceAccountToken: false
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: secrets-writer
  namespace: guardian-postflight
  labels:
    app.kubernetes.io/name: kargo
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: guardian-postflight
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: guardian-postflight-openbao
  labels:
    app.kubernetes.io/name: kargo
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: guardian-postflight
spec:
  provider:
    vault:
      server: https://guardian-openbao.tenant-guardian.svc:8200
      path: kv
      version: v2
      caProvider:
        type: Secret
        name: guardian-openbao-api-tls
        key: ca.crt
        namespace: tenant-guardian
      auth:
        kubernetes:
          mountPath: kubernetes
          role: guardian-reader-guardian-postflight
          serviceAccountRef:
            name: secrets-reader
            namespace: guardian-postflight
            audiences:
              - openbao
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: guardian-repo-creds
  namespace: guardian-postflight
  labels:
    app.kubernetes.io/name: kargo
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: guardian-postflight
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: guardian-postflight-openbao
  target:
    name: guardian-repo-creds
    creationPolicy: Orphan
    deletionPolicy: Retain
    template:
      type: Opaque
      engineVersion: v2
      metadata:
        labels:
          kargo.akuity.io/cred-type: git
      data:
        repoURL: https://github.com/guardian-intelligence/guardian
        githubAppID: "4206397"
        githubAppInstallationID: "144138265"
        githubAppPrivateKey: "{{ .githubAppPrivateKey }}"
  data:
    - secretKey: githubAppPrivateKey
      remoteRef:
        key: guardian/guardian-mgmt/guardian-postflight/promotion/github-app
        property: githubAppPrivateKey
