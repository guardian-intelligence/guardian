# Per-namespace secret scope for postflight-runner (OpenBao roles created by
# self-init): secrets-reader is what ESO authenticates as; secrets-writer
# exists only so an operator with custody kubeconfig access can mint a
# short-lived token scoped to writing this namespace's subtree. Neither SA
# automounts; nothing schedules as them.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: secrets-reader
  namespace: postflight-runner
  labels:
    app.kubernetes.io/name: postflight-runner-secrets
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: postflight-runner
automountServiceAccountToken: false
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: secrets-writer
  namespace: postflight-runner
  labels:
    app.kubernetes.io/name: postflight-runner-secrets
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: postflight-runner
automountServiceAccountToken: false
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: guardian-postflight-runner-openbao
  labels:
    app.kubernetes.io/name: postflight-runner-secrets
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: postflight-runner
spec:
  provider:
    vault:
      server: https://guardian-openbao.tenant-guardian.svc:8200
      path: kv
      version: v2
      caProvider:
        type: Secret
        name: guardian-openbao-api-tls
        key: ca.crt
        namespace: tenant-guardian
      auth:
        kubernetes:
          mountPath: kubernetes
          role: guardian-reader-postflight-runner
          serviceAccountRef:
            name: secrets-reader
            namespace: postflight-runner
            audiences:
              - openbao
---
# GitHub App credentials for the runner control plane. The kv values are
# seeded by the bootstrap importer (cmd/openbao_secret_import) from the
# custody env file's github_runner_app_prod_* keys; the secretKey names below
# are the exact env vars the control plane loads (services/postflight/
# controlplane/config.go), consumed via secretKeyRef in controlplane.yaml.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: github-app
  namespace: postflight-runner
  labels:
    app.kubernetes.io/name: postflight-runner-secrets
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: postflight-runner
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: guardian-postflight-runner-openbao
  target:
    name: github-app
    creationPolicy: Orphan
    deletionPolicy: Retain
    template:
      type: Opaque
      engineVersion: v2
  data:
    - secretKey: GITHUB_APP_ID
      remoteRef:
        key: guardian/guardian-mgmt/postflight-runner/github-app
        property: appId
    - secretKey: GITHUB_WEBHOOK_SECRET
      remoteRef:
        key: guardian/guardian-mgmt/postflight-runner/github-app
        property: webhookSecret
    - secretKey: GITHUB_APP_PRIVATE_KEY_PEM
      remoteRef:
        key: guardian/guardian-mgmt/postflight-runner/github-app
        property: githubAppPrivateKey
---
# The bearer shared with off-cluster runner hosts on the sync exchange. Unlike
# the App credentials this value originates operator-side, relayed once into
# the scope (bao kv put kv/guardian/guardian-mgmt/postflight-runner/hostd-sync
# secret=-), same shape as the postgres uri relay. Setting it in the control
# plane arms /api/v1/hostd/sync; each host carries the same value.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: hostd-sync
  namespace: postflight-runner
  labels:
    app.kubernetes.io/name: postflight-runner-secrets
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: postflight-runner
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: guardian-postflight-runner-openbao
  target:
    name: hostd-sync
    creationPolicy: Orphan
    deletionPolicy: Retain
    template:
      type: Opaque
      engineVersion: v2
  data:
    - secretKey: HOSTD_SYNC_SECRET
      remoteRef:
        key: guardian/guardian-mgmt/postflight-runner/hostd-sync
        property: secret
---
# The control-plane Postgres app lives in tenant-root (Cozystack constraint,
# see base/apps/postflight-controlplane-postgres.yaml); its operator-generated
# `uri` is relayed once into this scope by an operator with custody kubeconfig
# access (bao kv put kv/guardian/guardian-mgmt/postflight-runner/postgres uri=-,
# same shape as the analytics ClickHouse ingest-password relay) so ESO can
# materialize it here without a cross-namespace secretKeyRef.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres
  namespace: postflight-runner
  labels:
    app.kubernetes.io/name: postflight-runner-secrets
    app.kubernetes.io/part-of: guardian
    guardian.dev/secret-scope: postflight-runner
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: guardian-postflight-runner-openbao
  target:
    name: postgres
    creationPolicy: Orphan
    deletionPolicy: Retain
    template:
      type: Opaque
      engineVersion: v2
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: guardian/guardian-mgmt/postflight-runner/postgres
        property: uri
