# The public edge is the tenant-root ingress controller on the node public
# IPs (Cloudflare origins). It runs hostNetwork, so its traffic arrives with
# the reserved host/remote-node identities — never a pod identity — and the
# admit must be entity-based. This additive policy admits node-identity
# traffic to the privatecut-web pods. The -primary value is listed so a future
# Flagger takeover needs no policy change.
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: tenant-guardian-prod-allow-root-ingress-privatecut-web
  labels:
    app.kubernetes.io/part-of: guardian
    guardian.dev/product: privatecut
spec:
  endpointSelector:
    matchExpressions:
      - key: k8s:io.kubernetes.pod.namespace
        operator: In
        values:
          - tenant-guardian-prod
      - key: k8s:app.kubernetes.io/name
        operator: In
        values:
          - privatecut-web
          - privatecut-web-primary
  ingress:
    - fromEntities:
        - host
        - remote-node
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
