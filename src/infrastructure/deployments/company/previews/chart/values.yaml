# Required per release — set by .github/scripts/company-site-preview.sh:
pr: null # PR number (int)
commitSha: "" # PR head sha, surfaced as guardian:commit-sha for CI convergence polling
image:
  repository: ghcr.io/guardian-intelligence/company-site
  digest: "" # sha256:... — previews deploy by digest, never by tag

# Defaults:
domain: guardianintelligence.org
replicas: 1
