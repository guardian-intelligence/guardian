apiVersion: piraeus.io/v1
kind: LinstorSatelliteConfiguration
metadata:
  name: guardian-data-pool-ash-earth
  namespace: cozy-linstor
spec:
  nodeSelector:
    kubernetes.io/hostname: ash-earth
  storagePools:
    - name: data
      lvmPool:
        volumeGroup: data
      source:
        hostDevices:
          - /dev/mapper/luks2-r-guardian-data
---
apiVersion: piraeus.io/v1
kind: LinstorSatelliteConfiguration
metadata:
  name: guardian-data-pool-ash-wind
  namespace: cozy-linstor
spec:
  nodeSelector:
    kubernetes.io/hostname: ash-wind
  storagePools:
    - name: data
      lvmPool:
        volumeGroup: data
      source:
        hostDevices:
          - /dev/mapper/luks2-r-guardian-data
---
apiVersion: piraeus.io/v1
kind: LinstorSatelliteConfiguration
metadata:
  name: guardian-data-pool-ash-water
  namespace: cozy-linstor
spec:
  nodeSelector:
    kubernetes.io/hostname: ash-water
  storagePools:
    - name: data
      lvmPool:
        volumeGroup: data
      source:
        hostDevices:
          - /dev/mapper/luks2-r-guardian-data
