{
  "name": "@guardian/chunkies",
  "version": "0.1.0",
  "private": true,
  "description": "Game-agnostic replica host for journaled deterministic sims: binds a session-core wasm module and a replica wasm module over injected ports, with the game layered above through typed extension and projection doors. The name is a deliberate find-and-replaceable placeholder.",
  "files": [
    "src"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "vp exec tsc -p tsconfig.json",
    "test": "vp test run"
  },
  "dependencies": {
    "fflate": "catalog:"
  },
  "devDependencies": {
    "@guardian/chunkies-testkit": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
