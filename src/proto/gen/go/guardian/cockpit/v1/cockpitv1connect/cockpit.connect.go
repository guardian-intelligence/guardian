// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: guardian/cockpit/v1/cockpit.proto

// Wire contract for the status-cockpit live telemetry stream (the CPU/memory
// widget on the guardianintelligence.org homepage). Nodes are sampled at
// 10 Hz; the hub flushes one Frame per second carrying that second's ticks,
// so a viewer costs one message per second regardless of node count.
//
// Budget reasoning (measured at 10 Hz over 60 s on a Linux host under
// typical load): CPU% at 100 ms moves ~2 bits per sample, so per-tick values are delta-coded as zigzag varints in basis
// points — a packed repeated sint32 where steady-state deltas are one byte
// each. Memory measured ~0 bits of entropy at 100 ms, so it rides along
// absolute at 1 Hz. Steady state is ~20 B/node/s (~tens of bytes per frame);
// keyframes every 10 frames re-anchor absolutes and node names so a client
// that lost a frame resyncs within 10 s.
//
// Join semantics: the first Frame on a connection is a keyframe carrying the
// most recent full second of ticks, so clients start with a full playout
// buffer and render 1 s behind the live edge (constant-playout-delay jitter
// buffer). Tick timestamps are implicit: base_ts_ms + 100 ms per tick.
package cockpitv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/guardian-intelligence/guardian/src/proto/gen/go/guardian/cockpit/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CockpitStreamServiceName is the fully-qualified name of the CockpitStreamService service.
	CockpitStreamServiceName = "guardian.cockpit.v1.CockpitStreamService"
	// SamplerServiceName is the fully-qualified name of the SamplerService service.
	SamplerServiceName = "guardian.cockpit.v1.SamplerService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CockpitStreamServiceSubscribeProcedure is the fully-qualified name of the CockpitStreamService's
	// Subscribe RPC.
	CockpitStreamServiceSubscribeProcedure = "/guardian.cockpit.v1.CockpitStreamService/Subscribe"
	// SamplerServiceStreamTicksProcedure is the fully-qualified name of the SamplerService's
	// StreamTicks RPC.
	SamplerServiceStreamTicksProcedure = "/guardian.cockpit.v1.SamplerService/StreamTicks"
)

// CockpitStreamServiceClient is a client for the guardian.cockpit.v1.CockpitStreamService service.
type CockpitStreamServiceClient interface {
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.Frame], error)
}

// NewCockpitStreamServiceClient constructs a client for the
// guardian.cockpit.v1.CockpitStreamService service. By default, it uses the Connect protocol with
// the binary Protobuf Codec, asks for gzipped responses, and sends uncompressed requests. To use
// the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCockpitStreamServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CockpitStreamServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	cockpitStreamServiceMethods := v1.File_guardian_cockpit_v1_cockpit_proto.Services().ByName("CockpitStreamService").Methods()
	return &cockpitStreamServiceClient{
		subscribe: connect.NewClient[v1.SubscribeRequest, v1.Frame](
			httpClient,
			baseURL+CockpitStreamServiceSubscribeProcedure,
			connect.WithSchema(cockpitStreamServiceMethods.ByName("Subscribe")),
			connect.WithClientOptions(opts...),
		),
	}
}

// cockpitStreamServiceClient implements CockpitStreamServiceClient.
type cockpitStreamServiceClient struct {
	subscribe *connect.Client[v1.SubscribeRequest, v1.Frame]
}

// Subscribe calls guardian.cockpit.v1.CockpitStreamService.Subscribe.
func (c *cockpitStreamServiceClient) Subscribe(ctx context.Context, req *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.Frame], error) {
	return c.subscribe.CallServerStream(ctx, req)
}

// CockpitStreamServiceHandler is an implementation of the guardian.cockpit.v1.CockpitStreamService
// service.
type CockpitStreamServiceHandler interface {
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.Frame]) error
}

// NewCockpitStreamServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCockpitStreamServiceHandler(svc CockpitStreamServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	cockpitStreamServiceMethods := v1.File_guardian_cockpit_v1_cockpit_proto.Services().ByName("CockpitStreamService").Methods()
	cockpitStreamServiceSubscribeHandler := connect.NewServerStreamHandler(
		CockpitStreamServiceSubscribeProcedure,
		svc.Subscribe,
		connect.WithSchema(cockpitStreamServiceMethods.ByName("Subscribe")),
		connect.WithHandlerOptions(opts...),
	)
	return "/guardian.cockpit.v1.CockpitStreamService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CockpitStreamServiceSubscribeProcedure:
			cockpitStreamServiceSubscribeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCockpitStreamServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCockpitStreamServiceHandler struct{}

func (UnimplementedCockpitStreamServiceHandler) Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.Frame]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("guardian.cockpit.v1.CockpitStreamService.Subscribe is not implemented"))
}

// SamplerServiceClient is a client for the guardian.cockpit.v1.SamplerService service.
type SamplerServiceClient interface {
	StreamTicks(context.Context, *connect.Request[v1.StreamTicksRequest]) (*connect.ServerStreamForClient[v1.StreamTicksResponse], error)
}

// NewSamplerServiceClient constructs a client for the guardian.cockpit.v1.SamplerService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSamplerServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SamplerServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	samplerServiceMethods := v1.File_guardian_cockpit_v1_cockpit_proto.Services().ByName("SamplerService").Methods()
	return &samplerServiceClient{
		streamTicks: connect.NewClient[v1.StreamTicksRequest, v1.StreamTicksResponse](
			httpClient,
			baseURL+SamplerServiceStreamTicksProcedure,
			connect.WithSchema(samplerServiceMethods.ByName("StreamTicks")),
			connect.WithClientOptions(opts...),
		),
	}
}

// samplerServiceClient implements SamplerServiceClient.
type samplerServiceClient struct {
	streamTicks *connect.Client[v1.StreamTicksRequest, v1.StreamTicksResponse]
}

// StreamTicks calls guardian.cockpit.v1.SamplerService.StreamTicks.
func (c *samplerServiceClient) StreamTicks(ctx context.Context, req *connect.Request[v1.StreamTicksRequest]) (*connect.ServerStreamForClient[v1.StreamTicksResponse], error) {
	return c.streamTicks.CallServerStream(ctx, req)
}

// SamplerServiceHandler is an implementation of the guardian.cockpit.v1.SamplerService service.
type SamplerServiceHandler interface {
	StreamTicks(context.Context, *connect.Request[v1.StreamTicksRequest], *connect.ServerStream[v1.StreamTicksResponse]) error
}

// NewSamplerServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSamplerServiceHandler(svc SamplerServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	samplerServiceMethods := v1.File_guardian_cockpit_v1_cockpit_proto.Services().ByName("SamplerService").Methods()
	samplerServiceStreamTicksHandler := connect.NewServerStreamHandler(
		SamplerServiceStreamTicksProcedure,
		svc.StreamTicks,
		connect.WithSchema(samplerServiceMethods.ByName("StreamTicks")),
		connect.WithHandlerOptions(opts...),
	)
	return "/guardian.cockpit.v1.SamplerService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SamplerServiceStreamTicksProcedure:
			samplerServiceStreamTicksHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSamplerServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSamplerServiceHandler struct{}

func (UnimplementedSamplerServiceHandler) StreamTicks(context.Context, *connect.Request[v1.StreamTicksRequest], *connect.ServerStream[v1.StreamTicksResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("guardian.cockpit.v1.SamplerService.StreamTicks is not implemented"))
}
